#include "main.h"

/**
 * _isupper - returns whether a characte is uppercase
 * @c: the character to be checked
 * Return: 1 if c is uppercase, 0 if not
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
